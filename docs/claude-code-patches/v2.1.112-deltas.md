# Claude Code v2.1.112 system-prompt patch deltas

Lines: 18413 → 18412  (Δ -1)

## Hunk: bak L2082..2082  →  cur L2082..2082  [replace]

**Substring change** (showing ±80 char context):

```text
…it w1(Y,[q]);if(A===0)return!0}return!1}default:return!1}}function i88(){return`⟪/tmp⟫/claude-mcp-browser-bridge-${en1()}`}function DI8(){if(tS4()==="win32")return`\\…
```

**Replaced with:**

```text
…it w1(Y,[q]);if(A===0)return!0}return!1}default:return!1}}function i88(){return`⟪${z2()}⟫/claude-mcp-browser-bridge-${en1()}`}function DI8(){if(tS4()==="win32")return`\\…
```

## Hunk: bak L5079..5079  →  cur L5079..5078  [delete]

**Removed line:**

```text
- NEVER commit changes unless the user explicitly asks you to. It is VERY IMPORTANT to only commit when explicitly asked, otherwise the user will feel that you are being too proactive
```

## Hunk: bak L8251..8251  →  cur L8250..8250  [replace]

**Substring change** (showing ±80 char context):

```text
…ure flags or backwards-compatibility shims when you can just change the code."],⟪"Default to writing no comments. Only add one when the WHY is non-obvious: a hidden constraint, a subtle invariant, a workaround for a specific bug, behavior that would surprise a reader. If removing the comment wouldn't confuse a future reader, don't write it.",⟫`Don't explain WHAT the code does, since well-named identifiers already do that.…
```

**Replaced with:**

```text
…ure flags or backwards-compatibility shims when you can just change the code."],⟪⟫`Don't explain WHAT the code does, since well-named identifiers already do that.…
```

## Hunk: bak L8269..8269  →  cur L8268..8268  [replace]

**Substring change** (showing ±80 char context):

```text
…T("numeric_length_anchors",()=>"Length limits: keep text between tool calls to ≤⟪25 words. Keep final responses to ≤10⟫0 words unless the task requires more detail."),XT("brief",()=>h6A()),XT("focus_…
```

**Replaced with:**

```text
…T("numeric_length_anchors",()=>"Length limits: keep text between tool calls to ≤⟪60 words. Keep final responses to ≤25⟫0 words unless the task requires more detail."),XT("brief",()=>h6A()),XT("focus_…
```

